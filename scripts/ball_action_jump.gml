
    // ball_action_jump( jump_speed[num], scale_x[num], scale_y[num] );
    // make ball jump and scale
    
    ball_speed_v = -argument0;
    
    ball_scale_x = argument1;
    ball_scale_y = argument2;
    
    ball_jump = true;
